%nav.navbar.navbar-expand-md.navbar-dark{style: "background-color: #235DC9; color: white"}
  .container
    %a.navbar-brand.fw-bolder{:href => root_path} PadelApp
    - if not(current_page? reservations_path)
      %a.nav-link{:href => reservations_path} Mis Reservas
    - if user_signed_in?
      %a.nav-link{:href => destroy_user_session_path, data: {"turbo-method": :delete}} Cerrar Sesión
      %a.nav-link{:href => edit_user_registration_path}
        %i{class: "bi bi-person"}
    - else
      %a.nav-link{:href => new_user_session_path} Iniciar Sesión
      -# %button.btn.btn-secondary{:href => new_user_registration_path} Crear Cuenta
